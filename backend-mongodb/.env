# Database Configuration
MONGODB_URI=mongodb://localhost:27017/dao-management

# JWT Configuration
JWT_SECRET=super-secure-jwt-secret-for-development-only
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend Configuration (for CORS)
FRONTEND_URL=http://localhost:8080

# Email Configuration
# Pour configurer Gmail SMTP:
# 1. Activez l'authentification à deux facteurs sur votre compte Gmail
# 2. Générez un "App Password" dans les paramètres de sécurité Gmail
# 3. Remplacez SMTP_USER par votre email Gmail
# 4. Remplacez SMTP_PASS par le mot de passe d'application généré
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=fontoncredo@gmail.com
SMTP_PASS=
SMTP_FROM=noreply@2snd.fr

# Security
BCRYPT_ROUNDS=12
SESSION_SECRET=secure-session-secret-for-development
